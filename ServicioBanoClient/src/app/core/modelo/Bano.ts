export class Bano {

    id: number;
    identificador: string;
    estado: string;

    constructor(id: number, identificador: string, estado: string) {
        this.id = id;
        this.identificador = identificador;
        this.estado = estado;
    }
}